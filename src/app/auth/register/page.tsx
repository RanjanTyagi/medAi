import { RegisterForm } from '@/components/auth/RegisterForm'

export default function RegisterPage() {
  return <RegisterForm />
}

export const metadata = {
  title: 'Sign Up - MedAssist AI',
  description: 'Create your MedAssist AI account',
}